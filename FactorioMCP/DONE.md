# FactorioMCP — Completed Items

Items completed from the [TODO list](TODO.md).

---

## Features

- [x] **RCON Client (CPX 2)** — Source RCON protocol over TCP in `Rcon/RconClient.cs` and `Rcon/RconPacket.cs`. Supports authentication, command execution, and Lua execution via `/c` prefix. Uses `BinaryPrimitives` for little-endian packet framing, async I/O throughout, and proper cancellation token support.
- [x] **Factorio Service Layer (CPX 3)** — `Services/FactorioService.cs` wrapping RCON Lua calls with typed methods: Walk, StopWalking, GetPlayerPosition, GetInventory, Craft, GetCraftingQueue, PlaceEntity, MineEntityAt, GetNearbyEntities, GetResearchStatus, ExecuteRawLua. Uses `string.Create` with `InvariantCulture` for safe double formatting in Lua scripts.
- [x] **MCP Server Hosting & Wiring (CPX 2)** — Program.cs rewritten with Generic Host, DI registration of RconClient/FactorioService, `RconConnectionService` hosted service for RCON connection on startup from environment variables, MCP server with stdio transport and assembly-scanned tool discovery. Added `ModelContextProtocol` and `Microsoft.Extensions.Hosting` packages.
- [x] **Movement Tools (CPX 2)** — `Tools/MovementTools.cs` with MCP tools: `StopWalking`, `GetPlayerPosition`. Uses real physics-based walking via `player.walking_state`.
- [x] **Inventory & Crafting Tools (CPX 2)** — `Tools/InventoryTools.cs` with MCP tools: `GetInventory`, `Craft` (real crafting queue via `player.begin_crafting`), `GetCraftingQueue`.
- [x] **Entity Placement & Mining Tools (CPX 2)** — `Tools/EntityTools.cs` with MCP tools: `PlaceEntity` (place from inventory at coordinates with direction, validates inventory and position), `MineEntity` (mine entity at coordinates into inventory).
- [x] **World Scanning Tools (CPX 2)** — `Tools/WorldTools.cs` with MCP tools: `GetNearbyEntities` (entities within radius), `GetResearchStatus` (current research and progress). `GetPlayerPosition` already available in MovementTools.
- [x] **Blueprint & Ghost Support (CPX 4)** — `Services/BlueprintService.cs` + `Tools/BlueprintTools.cs` with 5 MCP tools: `PlaceGhostEntity` (place transparent construction plan via `create_entity{name="entity-ghost"}`), `PlaceBlueprintString` (import and build blueprint string via `cursor_stack.import_stack` + `build_from_cursor`), `GetGhostEntities` (scan for ghost entities with optional remote center), `CreateBlueprintFromArea` (capture area as exportable blueprint string via `create_blueprint` + `export_stack`), `RevokeGhostEntity` (destroy ghost entities at position). 53 unit tests + 6 integration tests.
- [x] **Inventory Management Tools (CPX 2)** — Added 3 MCP tools to `InventoryTools.cs` and 3 service methods to `FactorioService.cs`: `DropItems` (drop items onto ground via `spill_item_stack`), `TransferAllItems` (bulk transfer entity inventory to player via iterating slots), `GetEntityInventory` (inspect entity inventory contents with slot/empty counts via `get_contents`). 28 unit tests + 3 integration tests.
- [x] **Proximity Checks (CPX 2)** — `PlaceEntityAsync` validates `build_distance`, `MineEntityAtAsync` validates `reach_distance` before executing. Added `CheckDistanceAsync` service method and `CheckDistance` MCP tool in WorldTools for pre-flight range checks. Returns clear "Out of range" errors with distances when player is too far.
- [x] **Wait / Polling Mechanisms (CPX 3)** — `Tools/WaitTools.cs` with MCP tools: `WaitForCrafting` (poll until queue empties), `WaitForPosition` (poll until player reaches target within tolerance), `WaitForTicks` (poll until game ticks elapse), `GetGameTick` (current tick). All use configurable poll interval and timeout with JSON status responses (`complete`/`arrived`/`timeout`). Added `ScriptedRconClient` test double for polling tests. 20 new tests (98 total). TOOLS.md updated with new Wait & Polling section.
- [x] **Movement Pathfinding & Obstacle Avoidance (CPX 4)** — Player gets stuck when walking into entities. Implemented stuck detection + automatic detour in the Lua `on_tick` walking handler. Tracks player position each tick; after 10 ticks with no movement, switches to a perpendicular direction for 15 ticks to navigate around the obstacle, then resumes the original direction. Alternates detour sides (left/right) on repeated stuck events. Uses `storage.walk_state` for state persistence.
- [x] **Map / Terrain Scanning (CPX 3)** — Two new service methods and MCP tools: `ScanResources` scans for resource patches (ores, oil) within a radius, returning a summary per resource type (name, patch count, total amount, center coordinates). `ScanTiles` scans terrain tiles in an area, returning tile type counts (grass, sand, water, etc.). Both use `find_entities_filtered{type="resource"}` and `find_tiles_filtered` respectively. Added 13 unit tests.
- [x] **Player Interaction Tools (CPX 3)** — Three new service methods and MCP tools in `InteractionTools.cs`: `InsertItems` (insert items from player inventory into entity inventories — fuel drills, load furnaces, stock assemblers), `RemoveItems` (take items from entity inventories into player inventory — collect smelted plates, crafted items), `InspectEntity` (read entity status, inventory contents, burner fuel level, recipe, health, mining target). Supports 6 inventory types (fuel, furnace_source, furnace_result, chest, assembling_machine_input, assembling_machine_output). All tools prioritize non-resource entities when overlapping. Added 23 unit tests.
- [x] **Chat Message Reaction (CPX 3)** — Three new service methods and MCP tools in `Tools/ChatTools.cs`: `InitializeChatListener` (registers `on_console_chat` event handler via RCON that stores messages in `storage.chat_log`, idempotent), `GetChatMessages` (reads stored chat log with `sinceTick` filter for polling new messages, includes JSON escaping and `latest_tick` cursor), `SendChatMessage` (sends `[AI]`-tagged message via `game.print()` with Lua string escaping). Auto-initialized on RCON connection via `RconConnectionService`. Creative RCON-only workaround — no mod required. Added 18 unit tests.
- [x] **Goal Planner & State Machine (CPX 5)** — C#-side goal tracking system in `Services/GoalPlannerService.cs` with `Models/Goal.cs` data model and `Tools/GoalTools.cs` MCP tools. State machine lifecycle (Pending → Active → Completed/Failed/Suspended, with Resume support). Only one goal active at a time, auto-activation on creation. Ordered steps with sequential progression (Pending → InProgress → Completed/Skipped). JSON file persistence (`goals.json`, configurable via `FACTORIO_GOALS_FILE` env var) for resume across server restarts. Thread-safe via SemaphoreSlim. 9 MCP tools: `SetGoal`, `GetActiveGoal`, `GetAllGoals`, `AdvanceGoalStep`, `AddGoalSteps`, `CompleteGoal`, `FailGoal`, `SuspendGoal`, `ResumeGoal`. Added 36 unit tests.
- [x] **Energy Management Tools (CPX 3)** — Separate `Services/EnergyService.cs` and `Tools/EnergyTools.cs` for electric network inspection. Two service methods and MCP tools: `GetElectricNetwork` (finds nearest electric pole, reads `electric_network_statistics` flow counts with 5-second precision for production/consumption rates in watts, scans accumulators for charge levels, calculates satisfaction %), `InspectEntityPower` (entity-level power diagnostics — network connection, energy stored, buffer size, drain, generation via pcall). Uses `LuaFlowStatistics.get_flow_count` with `defines.flow_precision_index.five_seconds`, J/tick × 60 for watts conversion. Added 25 unit tests.
- [x] **Research Tools (CPX 2)** — Three MCP tools in `Tools/ResearchTools.cs`: `GetResearchStatus` (current research and progress, moved from WorldTools), `GetAvailableTechnologies` (lists technologies with prerequisites met, not yet researched, includes cost and ingredients), `StartResearch` (add technology to research queue via `force.add_research`, validates existence and not-already-researched, uses pcall for safety). Added `GetAvailableTechnologiesAsync` and `StartResearchAsync` to `FactorioService`. Added 21 unit tests.
- [x] **Raw Lua Execution Tool (CPX 1)** — `Tools/LuaTools.cs` with `ExecuteLua` MCP tool wrapping `FactorioService.ExecuteRawLuaAsync`. Includes safety warning about unsandboxed execution and game state corruption risk. Added 2 tests.
- [x] **Recipe & Technology Query Tools (CPX 2)** — `Tools/RecipeTools.cs` with three MCP tools: `GetRecipeDetails` (ingredients, products, crafting time, category, enabled status), `GetAvailableRecipes` (all unlocked recipes with category and energy), `GetTechnologyDetails` (prerequisites, effects/recipe unlocks, cost, ingredients, researched/enabled status). Added `GetRecipeDetailsAsync`, `GetAvailableRecipesAsync`, `GetTechnologyDetailsAsync` to `FactorioService`. Added 26 tests.
- [x] **Building Memory & State Tracking (CPX 4)** — `Services/BuildingMemoryService.cs` with `Models/TrackedBuilding.cs` data model and `Tools/BuildingTools.cs` MCP tools. Tracks placed buildings with JSON file persistence (`buildings.json`, configurable via `FACTORIO_BUILDINGS_FILE`). Thread-safe via SemaphoreSlim. Spatial queries: `GetAllBuildings`, `GetBuildingsNear` (radius search sorted by distance), `FindBuildingsByType`, `GetBuildingSummary` (counts by type), `UpdateBuildingLabel`, `ClearBuildingMemory`. Auto-tracking: `EntityTools.PlaceEntity` automatically records buildings on success, `MineEntity` automatically removes them. Position tolerance (0.5) handles floating-point imprecision. Added 36 unit tests (30 service + 6 integration/delegation).
- [x] **Remote Area Scanning Tools (CPX 2)** — Extended `GetNearbyEntities`, `ScanResources`, and `ScanTiles` with optional `centerX`/`centerY` parameters. When provided, scans center on the given coordinates instead of the player position, enabling long-range scouting without walking. Backward-compatible — omitting center defaults to player position. Updated `WorldTools.cs` MCP tool descriptions. Added 7 unit tests.
- [x] **WalkToPosition Tool (CPX 3)** — New `WalkToPosition` MCP tool in `MovementTools.cs` that walks toward a target position until arrival, stuck, or timeout. Auto-calculates 8-direction compass heading from current position, starts walking, polls position periodically, re-calculates direction for course correction, and detects stuck conditions (6 consecutive polls with <0.15 tile movement). Returns JSON with status (`arrived`/`stuck`/`timeout`), current position, target, distance, and tolerance. Orchestrates existing `WalkAsync`/`StopWalkingAsync`/`GetPlayerPositionAsync` service methods — no FactorioService changes needed. Added `CalculateDirection` internal static helper with atan2-based angle-to-direction mapping. Added 19 unit tests (8 direction calculation + 6 WalkToPosition behavior + 5 edge cases).
- [x] **MCP Resources (CPX 3)** — `Resources/GameStateResources.cs` with `[McpServerResourceType]` and 8 `[McpServerResource]` methods exposing read-only game state as MCP Resources: player position, player inventory, crafting queue, research status, available technologies, available recipes, electric network stats, and game tick. Registered via `.WithResourcesFromAssembly()` in Program.cs. Resources delegate to existing FactorioService/EnergyService methods — no new RCON commands needed. Added 9 tests (1 DI resolution + 8 delegation). Clients can read these via `read_resource` without making tool calls.
- [x] **Crafting Feasibility Check (CPX 2)** — Added `CheckCraftFeasibility` MCP tool in `RecipeTools.cs` and `CheckCraftFeasibilityAsync` service method in `FactorioService.cs`. Checks whether a recipe can be crafted with current inventory, reports max craftable count via `player.get_craftable_count(recipe)`, and provides per-ingredient breakdown (needed, available, missing). Validates recipe exists and is enabled. Added 14 unit tests + 1 integration test.
- [x] **Factory Status Command (CPX 3)** — Added `GetFactoryStatus` MCP tool in `Tools/StatusTools.cs` and `GetFactoryStatusAsync` service method in `FactorioService.cs`. Single RCON call gathers atomic game state snapshot: player position, full inventory (items/slots), crafting queue, research status/progress, nearby resource summary, nearby entity summary (grouped by type), and electric power status (production/consumption/satisfaction). Merges with C#-side building memory summary and active goal status. 12 unit tests + 6 integration tests.
- [x] **Pickup Items (CPX 1)** — `PickupItems` MCP tool in `InteractionTools.cs` and `PickupItemsAsync` in `FactorioService.World.cs`. Finds `item-entity` type entities within radius, inserts stacks into player inventory, returns summary of picked up items. 4 unit tests.
- [x] **Smart Entity Placement (CPX 2)** — `PlaceEntitySmart` MCP tool in `EntityTools.cs` and `PlaceEntitySmartAsync` in `FactorioService.Entity.cs`. Spiral search for valid placement using `can_place_entity`, validates inventory and build distance. Auto-tracks in building memory. 3 unit tests.
- [x] **Batch Entity Action Tools (CPX 3)** — `BatchTools.cs` with `MineEntityMultiple`, `InspectEntityMultiple`, `InsertItemsMultiple`, `RefuelEntityMultiple`. Accepts JSON arrays of targets, executes sequentially with fail-fast, returns aggregated results. 7 unit tests.
- [x] **Factory Analysis Tools (CPX 3)** — `FactoryTools.cs` with `FindUnpoweredEntities`, `FindIdleMachines`, `FindMissingInputs`. Service methods in `FactorioService.Factory.cs`. Diagnoses power issues, idle machines, and missing inputs. 7 unit tests.
- [x] **Craft & Factory Planning (CPX 3)** — `PlanCraft` MCP tool in `RecipeTools.cs` and `PlanCraftAsync` in `FactorioService.Planning.cs`. Recursively expands recipe tree showing all intermediates and raw materials with exact quantities. 3 unit tests.
- [x] **PreviewBeltPlacement Tool (CPX 2)** — `FlowService.PreviewBeltPlacementAsync` and `FlowTools.PreviewBeltPlacement`. Shows output/input sides, nearby inserters, existing entities, and `can_place_entity` check for a hypothetical belt tile.
- [x] **Flow Summary in Factory Status (CPX 2)** — Added `GetFlowSummaryAsync` to `FlowService` and integrated into `StatusTools.GetFactoryStatus` via `flowSummaryRadius` parameter. Returns inserter-mediated machine-to-machine connections and drill outputs as `item_flow` array in the factory status JSON. 6 unit tests.
- [x] **Entity Prototype Query (CPX 1)** — `GetEntityPrototype` MCP tool in `WorldTools.cs` and `GetEntityPrototypeAsync` in `FactorioService.World.cs`. Queries `prototypes.entity[name]` for tile_width, tile_height, max_health, type, and optional crafting_speed, mining_speed, energy_usage, collision_box. 12 unit tests.
- [x] **Batch Ghost Placement (CPX 2)** — `PlaceGhostBatch` MCP tool in `BlueprintTools.cs` and `PlaceGhostBatchAsync` in `BlueprintService.cs`. Places N ghosts in a single RCON call via Lua `for` loop over JSON array entries. Returns placed/skipped counts and per-entry errors. 10 unit tests.
- [x] **Area Occupancy Grid Query (CPX 2)** — `GetAreaOccupancy` MCP tool in `WorldTools.cs` and `GetAreaOccupancyAsync` in `FactorioService.World.cs`. Returns per-tile `{x, y, blocked, entity_name?, water?}` array for a rectangular area. Excludes resources from blocking, detects water tiles, limits to 10,000 tiles max. 9 unit tests.

---

## Improvements

- [x] **Fix GetGhostEntities direction output (CPX 1)** — `BlueprintService.GetGhostEntitiesAsync` now maps direction integers to human-readable names (north, east, etc.) using `dir_names` lookup, matching the pattern used by `GetNearbyEntitiesAsync`.
- [x] **RCON Reconnection & Resilience (CPX 2)** — `RconClient` now stores connection parameters, recreates `TcpClient` on connection loss, and retries with exponential backoff (up to 3 attempts). `ExecuteAsync` catches `IOException`/`SocketException`/`ObjectDisposedException` and auto-reconnects before retrying the command. `RconConnectionService` retries startup connection up to 5 times with backoff and logs warnings via `ILogger`. Thread-safe via `SemaphoreSlim`.
- [x] **Structured Lua Responses (CPX 2)** — All `FactorioService` methods now return JSON via `rcon.print()` instead of ad-hoc text. Responses use flat keys (e.g. `{"success":true,"entity":"stone-furnace","x":5,"y":-2}`) for reliable AI parsing. Error responses include structured `error` codes (`out_of_range`, `invalid_position`, `missing_item`, `no_entity`). TOOLS.md updated with all JSON response examples.
- [x] **MCP Tool Call Logging (CPX 2)** — Added `McpRequestFilter<CallToolRequestParams, CallToolResult>` in `Program.cs` via `WithRequestFilters` + `AddCallToolFilter`. Logs tool name and JSON-serialized arguments to stderr on invocation and completion. Uses `Console.Error` to avoid interfering with MCP stdio transport on stdout. Zero per-tool changes — all tools are covered automatically by the SDK filter pipeline.
- [x] **RCON Client Connection Logging (CPX 2)** — Added `ILogger<RconClient>` to `RconClient` with optional constructor parameter (defaults to `NullLogger` for test compatibility). Logs: initial connection attempts (Debug), successful authentication (Information), connection loss during command execution (Warning with exception), each reconnection attempt with attempt/max count (Information), reconnection success (Information), per-attempt failures with backoff timing (Warning), final reconnection failure (Error), and connection close events (Debug).
- [x] **Command Queuing & Sequencing (CPX 3)** — `Services/GameCommandQueue.cs` serializes game operations via `SemaphoreSlim` to prevent race conditions when multiple MCP tool calls arrive concurrently. All tool classes (MovementTools, InventoryTools, EntityTools, WorldTools, InteractionTools, WaitTools, ChatTools, ResearchTools, LuaTools, RecipeTools, EnergyTools, BuildingTools) use `queue.ExecuteAsync()` for exclusive execution. Registered as singleton in DI. Added 5 unit tests.
- [x] **Goal Planner Timestamps (CPX 1)** — Added `UpdatedAt` property to `Goal` model. All goal tool responses now include `CreatedAt`/`UpdatedAt` timestamps, and terminal-state responses (Complete, Fail) also include `CompletedAt`. `UpdatedAt` is set to `DateTime.UtcNow` on every mutation (AdvanceStep, AddSteps, Complete, Fail, Suspend, Resume). Added 12 unit tests.
- [x] ~~**RCON Multi-Packet Response Handling (CPX 2)**~~ — *Reverted: Factorio's RCON does not support receiving type 0 (ResponseValue) packets from clients. Replaced with single-packet reads. See Bug Fixes.*
- [x] **Inserter Placement Guidance (CPX 3)** — Added `PreviewInserterPlacement` MCP tool in `EntityTools.cs` and `PreviewInserterPlacementAsync` service method in `FactorioService.cs`. Previews what an inserter would pick up from and drop to at a given position and direction without placing anything. Lua script calculates pickup/drop positions via direction offset table, scans for entities at both positions, checks `can_place_entity`. Enhanced `PlaceEntity` tool description with inserter direction semantics. Added "Pattern: Placing Inserters" section to PROMPT.md with direction table, workflow, example, and common mistakes. 13 unit tests + 3 integration tests.
- [x] **Belt Placement & Routing Guidance (CPX 3)** — Added `BeltPlannerService` (pure C# geometry) and `BeltTools.cs` with `PlanBeltRoute` (calculates belt tile positions and directions for straight and L-shaped routes) and `GetBeltDirectionHelp` (direction reference guide). Enhanced `PlaceEntity` description with belt tips. Updated PROMPT.md and TOOLS.md. 22 unit tests + 3 integration tests.
- [x] **Realistic Mining Duration (CPX 3)** — Added `MiningService` with `StartMiningResourceAsync`, `GetMiningStatusAsync`, `StopMiningAsync`, and `MineResourceAsync` using `player.mining_state` for tick-based resource mining. Added `MineResource` MCP tool in `EntityTools.cs` for realistic ore extraction (one unit at a time with polling). Updated `MineEntityAtAsync` to redirect resource entities to `MineResource`. 25 unit tests + 1 integration test.
- [x] **Inventory Full Detection & Handling (CPX 3)**
- [x] **Rotate Entity (CPX 1)** — Added `RotateEntity` MCP tool in `EntityTools.cs` and `RotateEntityAsync` service method in `FactorioService.Entity.cs`. Rotates a building at given coordinates using Lua `entity.rotate()`. Validates proximity, prioritizes non-resource entities. Automatically updates building direction in `BuildingMemoryService` via new `UpdateBuildingDirectionAsync` method. Changed `TrackedBuilding.Direction` from `init` to `set`. Supports reverse (counter-clockwise) rotation.
- [x] **Target Selection Helpers (CPX 2)** — Added `Tools/TargetTools.cs` with 3 MCP tools: `FindNearest` (find nearest entity by name or type within radius via Lua `find_entities_filtered`, searches by name first then falls back to type), `FindBestResourcePatch` (clusters resource entities into patches using 8-tile grid cells, ranks by `total_amount / (distance + 10)` heuristic, returns best patch with up to 3 alternatives), `GetClosestBuildingOfType` (queries `BuildingMemoryService` for nearest tracked building, gets player position for distance calculation). Added `FindNearestEntityAsync` and `FindBestResourcePatchAsync` to `FactorioService.World.cs`, `GetClosestBuildingOfTypeAsync` to `BuildingMemoryService`. 28 unit tests.
- [x] **Smart Navigation (CPX 3)** — Added `Tools/NavigationTools.cs` with 3 compound MCP tools: `MoveToEntity` (find nearest entity by name/type via `FindNearestEntityAsync` → walk to it), `MoveToResource` (find best resource patch via `FindBestResourcePatchAsync` → walk to center), `MoveToBuilding` (search AI building memory by label then entity name → walk to closest). Each combines target finding + walking into a single call, eliminating multi-step scan→parse→walk sequences. Added `FindClosestBuildingAsync` to `BuildingMemoryService` (label-first search with case-insensitive contains, falls back to entity name). Walking logic reuses `MovementTools.CalculateDirection` with same stuck detection and obstacle avoidance. 22 unit tests (12 navigation + 9 building search + 1 DI resolution).
- [x] **Reactive Waiting (CPX 3)** — Three type-safe reactive wait tools in `WaitTools.cs` backed by service methods in `FactorioService.Wait.cs`: `WaitForItemCount` (poll player inventory until item count ≥ target), `WaitForEntityStatus` (poll entity `defines.entity_status` until it matches target like "working"/"idle"/"no_fuel", early-exits on missing entity), `WaitForEntityInventory` (poll entity inventory by type — fuel/furnace_source/furnace_result/chest/assembling_machine_input/assembling_machine_output — until item count ≥ target, early-exits on invalid inventory errors). All return structured JSON with "satisfied"/"timeout"/"error" status. Added `TryParseJsonInt`/`TryParseJsonString` helpers. 28 unit tests.
- [x] **Semantic Area Perception (CPX 3)** — Added `Tools/PerceptionTools.cs` with 3 MCP tools backed by service methods in `FactorioService.World.cs`: `SummarizeArea` (structured overview of resources, machines grouped by type with working/idle counts, threats/enemies, and free-space estimate within a radius), `WhatAmILookingAt` (directional raycast-style query using a narrow cone to report entities sorted by distance along a compass direction), `FindBuildableArea` (spiral-outward search for a rectangular area free of structures and water tiles, suitable for factory placement). All support optional center coordinates for remote scanning. 25 unit tests.
- [x] **High-Level Task Primitives (CPX 4)** — Added `Tools/TaskTools.cs` with 3 compound MCP tools that collapse 10–20 atomic calls into 1: `GatherResource` (FindBestResourcePatch → walk → FindNearestEntity → MineResource with realistic tick-based mining), `RefuelEntity` (distance check → walk if needed → InsertItems fuel), `Smelt` (find furnace via building memory or world search → walk → insert ore into furnace_source → insert fuel → poll furnace status via custom Lua → collect output from furnace_result). Furnace finding searches all types (stone-furnace, steel-furnace, electric-furnace) in building memory, falls back to world entity search. Walk logic includes stuck detection and course correction. 18 unit tests.
- [x] **Smart Inserter Placement (CPX 3)** — Added `PlaceInserter` and `InsertBetween` MCP tools in `EntityTools.cs` with `PlaceInserterAsync` and `InsertBetweenAsync` service methods in `FactorioService.Entity.cs`. `PlaceInserter` accepts a target entity position, side (top/bottom/left/right), and flow direction (inbound/outbound), uses `bounding_box` to calculate exact inserter tile offset and direction. `InsertBetween` auto-places an inserter in the 1-tile gap between two entities with correct orientation using midpoint and direction vector calculation. Both auto-track in building memory. 19 unit tests.
- [x] **Vision Screenshot (CPX 3)** — Added `VisionService.cs` and `Tools/VisionTools.cs` with `TakeScreenshot` MCP tool returning `CallToolResult` with `ImageContentBlock` (PNG screenshot) and `TextContentBlock` (structured map legend). Uses Factorio's `game.take_screenshot` with `show_entity_info=true` (Alt mode). Draws temporary rendering overlays: color-coded entity bounding boxes (by type — inserter/belt/assembler/furnace/miner/chest/pole/pipe/lab), numbered labels, and inserter pickup→drop direction arrows via `rendering.draw_rectangle`/`draw_text`/`draw_line` with `time_to_live=120`. Collects structured JSON map legend with entity positions, directions, inserter pickup/drop coordinates, assembler recipes, and machine status. Supports custom center coordinates, zoom, and resolution. Calculates visible-area scan radius from resolution and zoom (`pixels / (32 * zoom)`). Uses `game.set_wait_for_screenshots_to_finish()` for reliable file I/O. Reads screenshot from Factorio's `script-output/ai-player/vision.png`. 34 unit tests.
- [x] **Utility Tools (CPX 2)** — Added `Tools/UtilityTools.cs` with 3 MCP tools: `GetReachableEntities` (filter entities within reach distance, optional type filter, returns entity info with distances), `CountItemInWorld` (count an item across all nearby containers — chests, furnaces, assemblers — plus player inventory, with per-container breakdown), `EstimateTravelTime` (estimate walk time to a position using player speed and straight-line distance). Service methods added to `FactorioService.World.cs`. 29 unit tests.
- [x] **Inventory Intelligence (CPX 2)** — Added 2 MCP tools to `InventoryTools.cs`: `GetInventorySummary` (condensed key-value inventory format — `{"items":{"iron-plate":50,...}}` — much fewer tokens than full inventory dump), `EnsureItem` (check if player has enough of an item, if not report whether it can be crafted and what ingredients are short). Service methods added to `FactorioService.Inventory.cs`. 10 unit tests.
- [x] **Collision Slot Query (CPX 2)** — Added `GetAvailableSlots` MCP tool to `EntityTools.cs` and `GetAvailableSlotsAsync` to `FactorioService.Entity.cs`. Finds entity at a position, computes bounding box, checks all adjacent perimeter tiles for collision using `can_place_entity`, returns slots with direction (north/south/east/west), blocked status, and availability counts. 7 unit tests.
- [x] **Ghost Placement Validation (CPX 3)** — Added `ValidateGhostPlacements` MCP tool to `BlueprintTools.cs` and `ValidateGhostPlacementsAsync` to `BlueprintService.cs`. Scans ghost entities in an area, validates each for placement issues: blocked positions (via `can_place_entity`), inserters with no pickup target, inserters with no drop target. Returns corrective error messages so the LLM can fix its plan before committing real entities. Checks both real entities and other ghosts at inserter pickup/drop positions. 16 unit tests.
- [x] **FindBuildableArea Ore Patch Avoidance (CPX 2)** — Added `allowOrePatches` parameter (default `false`) to `FindBuildableAreaAsync` and `FindBuildableArea` MCP tool. When false, ore patches count as blocking entities. When true (for mining drills), ore patches are excluded from blocking checks. 2 unit tests.

---

## Documentation

- [x] **README.md (CPX 1)** — Project overview, prerequisites, setup instructions, env var configuration, how to run, MCP client connection examples (Claude Desktop, VS Code Copilot), available tools reference, project structure.
- [x] **Available Tools Reference (CPX 1)** — `TOOLS.md` with detailed documentation for all 15 MCP tools: parameters, types, return values, example outputs, and AI prompt engineering tips. Linked from README.
- [x] **Lua API Audit** — Verified all Lua snippets in `FactorioService.cs` against bundled Factorio API HTML docs. Confirmed correct: LuaPlayer (walking_state, build_distance, reach_distance, begin_crafting, crafting_queue, get_main_inventory, get_item_count, remove_item), LuaSurface (find_entities_filtered, can_place_entity, create_entity), LuaForce (current_research, research_progress), LuaEntity (mine), LuaRCON (print), defines (direction). All calls match the API reference.
- [x] **Example mcp.json (CPX 1)** — Added `.vscode/mcp.json` pre-configured with `dotnet run --project` command and all default environment variables (`FACTORIO_RCON_HOST`, `FACTORIO_RCON_PORT`, `FACTORIO_RCON_PASSWORD`). Uses `${workspaceFolder}` for portable project path. Updated README VS Code section to reference the bundled file with manual setup in collapsible details.

- [x] **Use `/silent-command` Instead of `/c` (CPX 1)** — Switched `RconClient.ExecuteLuaAsync` from `/c` to `/silent-command` so Lua commands don't appear in the game chat log. Updated all unit test assertions from `/c` prefix to `/silent-command` prefix.
- [x] **LuaAPI Reference (CPX 1)** — Added `LUA_API.md` documenting the bundled `LuaAPI/` folder (Factorio 2.0.76, API v6). Covers folder structure, key runtime classes (LuaPlayer, LuaSurface, LuaEntity, LuaForce, LuaRCON, LuaGameScript, LuaRecord, LuaFlowStatistics), key defines, events, RCON-specific notes, and machine-readable JSON API references.
- [x] **LM Studio Setup Guide (CPX 1)** — Added LM Studio connection instructions to README under "Connecting an MCP Client" with MCP server JSON configuration, recommended model suggestion, and notes about local model tool-calling limitations.
- [x] **Architecture Decision Records (CPX 1)** — Created `ADR.md` documenting three key decisions: ADR-001 RCON over Mod API (zero install, no version coupling, sufficient capability), ADR-002 Realistic AI Constraints (meaningful gameplay, transferable strategies, emergent complexity), ADR-003 Stdio Transport (universal client support, simple deployment, security).
- [x] **AI Prompt Guide — PROMPT.md & TOOLS.md Update (CPX 2)** — Updated `PROMPT.md` with `CheckCraftFeasibility` tool (added to tool table, updated crafting pattern to use feasibility checks instead of manual inventory inspection, updated common mistakes section). Regenerated `TOOLS.md` from scratch to reflect all current MCP tools (55+ tools across 13 sections), replacing the outdated version that still referenced removed tools like `WalkForDuration` and was missing `WalkToPosition`, `CheckCraftFeasibility`, Blueprint tools, Building Memory tools, and others.

---

## Bug Fixes

- [x] **RCON `game.player` is nil fix** — Discovered via live testing that `game.player` returns `nil` in RCON context (both singleplayer and dedicated server). Replaced all 12 Lua snippets in `FactorioService.cs` to use `game.connected_players[1]` which correctly resolves the first connected player. All service methods (Walk, StopWalking, GetPlayerPosition, GetInventory, Craft, GetCraftingQueue, PlaceEntity, MineEntity, GetNearbyEntities, CheckDistance, GetResearchStatus, WaitForPosition) now work against a live Factorio instance. Added 14 live integration tests in `LiveGameTests.cs` confirming all tools function correctly.
- [x] **`MineNearbyResource_AddsToInventory` integration test fix** — Test picked the first mineable entity (not closest), walked once without re-checking distance, and crashed on empty RCON response. Fixed to pick the closest entity, retry walking up to 5 times with distance re-checks, and handle empty mine responses gracefully.
- [x] **`MineEntityAtAsync` Factorio 2 API fix** — `entity.mine{inventory=player.get_main_inventory()}` fails in Factorio 2 (`"inventory" must be a script inventory or entity inventory`). Switched to `player.mine_entity(e, true)` which correctly mines as-if-player, handles inventory transfer automatically, and raises proper game events (`on_player_mined_entity`, etc.).
- [x] **`WalkAndStop_MovesPlayer` Flaky Test Fix** — Integration test failed when the player was blocked by nearby entities. Updated to try multiple directions (south, east, north, west) and succeed if the player moves in any of them. Proper diagnostic output shows how far the player moved in each attempt.

- [x] **Factorio 2 `global` → `storage` Migration** — Factorio 2 removed the `global` table and replaced it with `storage`. Fixed all Lua scripts using `global.walk_state` (in `WalkAsync` and `StopWalkingAsync`) to use `storage.walk_state` instead. This was causing `attempt to index global 'global' (a nil value)` errors during walking.
- [x] **Factorio 2 `game.active_mods` API Fix** — `game.active_mods` was removed in Factorio 2. Fixed `RawDiagnostics_CheckGameState` integration test to use `script.active_mods['base']` instead.
- [x] **`CraftAsync` Reports Success When Nothing Was Queued (CPX 1)** — `begin_crafting` returned 0 but status was `"crafting"`. Now returns `"status":"no_materials"` when `queued` is 0.
- [x] **`CraftAsync` Non-JSON Error on Invalid Recipe (CPX 1)** — Invalid recipe names caused raw Lua errors. Wrapped `begin_crafting` in `pcall` and returns `{"status":"error","error":"unknown_recipe"}` on failure.
- [x] **`MineEntityAtAsync` Cannot Mine Resource Entities (CPX 2)** — `player.mine_entity()` fails on resource entities in Factorio 2. Now detects resource-type entities and manually harvests (reads amount and products from prototype, inserts into inventory, destroys entity).
- [x] **`MineEntityAtAsync` Picks Wrong Entity When Overlapping (CPX 2)** — `find_entities_filtered` returned entities in arbitrary order, often picking ore under a drill. Now sorts entities to prioritize non-resource entities first.
- [x] **RCON Sentinel Packet Uses Invalid Message Type (CPX 2)** — `ExecuteCoreAsync` sent a sentinel `ResponseValue` (type 0) packet for multi-packet response detection (Valve Source RCON technique). Factorio's RCON only accepts type 2 (`ExecCommand`) and type 3 (`Auth`) from clients, rejecting type 0 with `RCON received an invalid message type 0`. Removed sentinel mechanism; Factorio returns complete responses in a single packet, so `ExecuteCoreAsync` now simply sends the command and reads one response.
- [x] **InsertItems/RemoveItems Malformed Lua JSON (CPX 2)** — `InsertItemsAsync` and `RemoveItemsAsync` had malformed Lua string concatenation in the JSON output (`,"requested":{{count}}..'}')`) — after C# `$$` interpolation the number ended up inside a Lua string literal, causing a syntax error that prevented the entire script from executing. Fixed to `,"requested":'..{{count}}..'}'` so the count value is properly concatenated as a Lua number.
- [x] **Inserter Direction Semantics Bug (CPX 3)** — Inserter `direction` = DROP direction but the AI interpreted it as pickup, causing wrong placements. Fixed by: (1) enhancing `InspectEntityAsync` to report entity direction and, for inserters, calculated pickup/drop tile positions with entities at each side; (2) enhancing `GetNearbyEntitiesAsync` to include entity direction in output; (3) strengthening `PlaceEntity` and `PreviewInserterPlacement` tool descriptions with clearer "direction = where items go TO" mnemonic; (4) added integration test verifying inserter preview pickup/drop positions for all cardinal directions.

- [x] **Factorio 2 `.amount` Property Throws on Non-Resource Entities (CPX 2)** — Lua code `if e.amount then` / `if best.amount then` throws "Entity is not resource." in Factorio 2 (unlike Factorio 1 which returned nil). Affected `FindNearestEntityAsync` (line 304) and `LookInDirectionAsync` (line 531) in `FactorioService.World.cs`, breaking `FindNearest`, `MoveToEntity`, and `WhatAmILookingAt` for any non-resource entity. Fixed by replacing `if e.amount then` with `if e.type == "resource" then` before accessing `.amount`. Other `.amount` usages were already safe (guarded by `type="resource"` filters in `ScanResources`, `GetFactoryStatus`, `SummarizeArea`, `FindBestResourcePatch`, `MineEntity`, `MiningService`).
- [x] **WalkToPosition Oscillation / Infinite Loop (CPX 3)** — Player oscillated between two points instead of reaching the target. Root cause: two competing control loops — Lua `on_tick` handler (60Hz) and C# polling loop (2Hz) both tried to control direction and handle stuck detection simultaneously. The on_tick handler forced `walking_state` every tick, while C# recalculated direction every 0.5s and called `WalkAsync` which reset the Lua state. This created a feedback loop where the two systems fought each other. Fixed by: (1) removing the entire Lua `on_tick` handler from `WalkAsync` — now just sets `walking_state` once per call; (2) moving obstacle avoidance entirely into the C# polling loop in `WalkToPosition`/`WalkToTargetAsync` with perpendicular detour logic (try side 1, then side 2, each for 6 polls before retrying target direction); (3) added `GetPerpendicularDirection` helper with 12 unit tests; (4) updated all 3 copies of `WalkToTargetAsync` in `MovementTools`, `NavigationTools`, and `TaskTools`.
- [x] **Factorio 2 `prototype.max_health` Removed (CPX 1)** — `InspectEntityAsync` accessed `e.prototype.max_health` which throws "LuaEntityPrototype doesn't contain key max_health" in Factorio 2. The property moved to `LuaEntity.max_health` directly. Fixed by replacing `e.prototype.max_health` with `e.max_health`.
- [x] **Factorio 2 `walking_state` Doesn't Persist (CPX 2)** — In Factorio 2, `player.walking_state` does NOT persist across ticks — setting it once only makes the player walk for one game tick (~0.02 tiles). The previous fix (removing the `on_tick` handler) broke walking entirely. Fixed by restoring the `on_tick` handler with a clean separation of concerns: `on_tick` handler only reads direction from `storage.walk_state` and applies `walking_state` every tick (no autonomous direction logic), C# polling loop changes direction by updating `storage.walk_state.direction` via subsequent `WalkAsync` calls (no handler reinstall). `StopWalkingAsync` clears `storage.walk_state` and removes the handler. Player reference looked up dynamically inside handler (`game.connected_players[1]`) to avoid stale closure references.
- [x] **Factorio 2 `mining_state` Doesn't Persist (CPX 3)** — Same root cause as `walking_state`: `player.mining_state` does NOT persist across ticks in Factorio 2. Mining would get interrupted after one tick, and the player could barely mine 1–2 resources before the polling loop timed out or got lucky. Fixed with a unified `on_tick` handler architecture: single handler checks both `storage.walk_state` (for walking) and `storage.mine_state` (for mining) every tick, applying `walking_state` and `mining_state` respectively. Handler constants (`InstallOnTickHandler`, `RemoveOnTickIfIdle`) are `internal const` on `FactorioService`, shared by both `FactorioService` and `MiningService`. Handler is installed by either system and removed only when both are idle (`storage.walk_state == nil and storage.mine_state == nil`). Prevents `script.on_event` clobbering since Factorio replaces any existing handler. Additionally, `update_selected_entity` must be called every tick before `mining_state` — Factorio 2 requires the entity to be re-selected each tick for mining to continue. `StartMiningResourceAsync` now stores the entity's exact position (not the user-supplied position) for reliable re-selection. 7 new tests (5 mining + 2 movement).
- [x] **Factorio 2 `e.mining_target` Throws on Non-Mining-Drill (CPX 1)** — `InspectEntityAsync` accessed `e.mining_target` which throws on non-mining-drill entities in Factorio 2 (same pattern as `.amount`). Fixed by guarding with `if e.type == "mining-drill" and e.mining_target then` before accessing the property.
- [x] **Lua Number Concat Malformed Number (CPX 2)** — When C# `$$` string interpolation formats a whole number without a decimal point (e.g. `100`), Lua code like `..100..` is ambiguous (number followed by `..` concat operator). This caused "malformed number" Lua errors intermittently. Fixed 5 locations across 4 files: `EnergyService.cs` (`GetElectricNetwork` radius — used `string.format("%.1f", ...)`), `BlueprintService.cs` (ghost scan radius — paren wrapping `..({{radius}})..`), `FactorioService.Entity.cs` (InsertItems/RemoveItems count — paren wrapping), `MiningService.cs` (GetMiningStatus initialAmount — paren wrapping).
- [x] **Factorio 2 `tech.effects` Moved to Prototype (CPX 1)** — `GetTechnologyDetailsAsync` accessed `tech.effects` which throws "LuaTechnology doesn't contain key effects" in Factorio 2. The property moved to `LuaTechnology.prototype.effects`. Fixed by replacing `tech.effects` with `tech.prototype.effects`.
- [x] **Factorio 2 `tech.research_progress` Moved to Force (CPX 1)** — `GetResearchStatusAsync` and `GetFactoryStatusAsync` accessed `tech.research_progress` which throws "LuaTechnology doesn't contain key research_progress" in Factorio 2. The property moved to `LuaForce.research_progress`. Fixed by replacing `tech.research_progress` with `force.research_progress` in both methods.
- [x] **RCON Packet ID Mismatch Resilience (CPX 2)** — `ExecuteCoreAsync` now validates that the response packet ID matches the request ID. If a stale packet is in the buffer (from auth handshake or previous command), it drains up to 5 mismatched packets before throwing, logging each with a warning. `ConnectCoreAsync` now handles the Source RCON auth flow correctly — reads up to 2 packets to account for servers that send an empty `ResponseValue` before the `AuthResponse`. Previously, a single stale packet would cause all subsequent reads to be off-by-one, returning the previous command's response indefinitely.
- [x] **Walking Overshoot Oscillation (CPX 2)** — When walking to a close target (within ~5 tiles), the player oscillated left-right indefinitely. Root cause: the on_tick handler applies `walking_state` every tick (60Hz), but the C# polling loop only checks position every 500ms. In 30 ticks the player moves ~4.5 tiles, overshooting a 2-tile-away target. The direction then reverses, causing another overshoot in the opposite direction. Fixed by adding overshoot detection: when the player is moving and the recalculated direction reverses (opposite or near-opposite), and the distance is within 3× tolerance, the walk is stopped and treated as arrived. Added `AreOppositeDirections` helper to `MovementTools` covering exact opposites and 135° near-opposites. Applied to both `WalkToPosition` (MovementTools) and `WalkToTargetAsync` (TaskTools). 22 new tests (20 direction tests + 2 overshoot behavior tests).
- [x] **Stale Building Memory After Manual Removal (CPX 3)** — Building memory tracked entities placed by the AI but had no way to detect removal by players, enemies, or mods, causing phantom entries. Added `ValidateBuildingMemory` MCP tool and `ValidateBuildingMemoryAsync` service method that sends a single batched Lua script via RCON to check all tracked positions with `find_entities_filtered`, returning comma-separated 1/0 results. Prunes stale entries and persists changes. Added `RconClient` as optional dependency to `BuildingMemoryService`. 16 new tests (8 validation behavior + 2 Lua generation + 6 response parsing).
- [x] **RemoveItemsAsync Reports `inserted` as `removed` (CPX 1)** — `FactorioService.Entity.cs`: JSON response reported `"removed":'..inserted` instead of the actual `removed` count. When player inventory was full and `inserted < removed`, excess items were put back but the wrong count was reported. Fixed to report both `"removed":'..removed` (gross from entity) and `"transferred":'..inserted` (net to player).
- [x] **Electric Network Stats Nil Crash (CPX 1)** — `FactorioService.World.cs` `GetFactoryStatusAsync`: iterated `stats.output_counts` and `stats.input_counts` without nil guards. When no producers or consumers existed on the network, these could be nil causing a Lua runtime error. Added `if stats.output_counts then ... end` and `if stats.input_counts then ... end` guards.
- [x] **Wait Tools MCP Protocol Timeout (CPX 2)** — All wait tools (`WaitForCrafting`, `WaitForPosition`, `WaitForTicks`, `WaitForItemCount`, `WaitForEntityStatus`, `WaitForEntityInventory`) could throw `MCP error -32001: Request timed out` when the tool's internal timeout exceeded the MCP SDK's protocol-level timeout. Added `OperationCanceledException` catch in each `WaitTools` wrapper to return clean `{"status":"timeout","reason":"cancelled"}` JSON instead of propagating the exception as an MCP error.
- [x] **RCON Stale "nothing" Response Auto-Recovery (CPX 3)** — After extended use or game pause/unpause, the RCON connection would return `"nothing"` for all commands indefinitely without throwing exceptions, so auto-reconnect never triggered. Added consecutive "nothing" response tracking to `ExecuteAsync` — after 3 consecutive `"nothing"` responses, forces a full reconnect. Added `ForceReconnectAsync` method for manual recovery and `ReconnectRcon` MCP tool in `LuaTools`. Updated `LuaTools` constructor to accept `RconClient` for reconnect access.
- [x] **Pathfinding Stuck — walking_state Resets Every Tick (CPX 3)** — `walk_to_position`, `move_to_entity`, `move_to_resource`, and all navigation tools returned `stuck` immediately because `walking_state` in Factorio 2 only persists for a single game tick. The C# poll loop (150ms ≈ 9 ticks) set `walking_state` once, but the player only moved for 1 tick out of 9, making negligible progress that triggered stuck detection. Fixed by adding a Lua `on_tick` handler that reads `storage.walk_dir` and continuously re-applies `walking_state` every tick. C# controls direction by writing to `storage.walk_dir` instead of setting `walking_state` directly. Combined the walking on_tick handler with the existing mining on_tick handler (`storage.mine_state`) into a shared `LuaOnTickHandler` constant on `FactorioService` to prevent `script.on_event` clobbering between subsystems. The handler is never removed after installation to avoid the reinstallation flag (`_pathHandlerInstalled`) getting out of sync. Updated `MiningService.StopMiningAsync` to also keep the handler installed.
- [x] **Entity Selection Picks Player Character (CPX 1)** — `insert_items`, `mine_entity`, `rotate_entity`, and other entity interaction tools could select the player's own character entity instead of the intended target when the player was standing near the target. The `sort_entities()` Lua helper only deprioritized resource entities but didn't filter out the character. Fixed by adding character entity filtering to `sort_entities()` — it now removes the player's character from the entity list before sorting, preventing accidental self-selection across all tools that use the helper.
- [x] **EnergyService Test — Stale `table.sort` Assertion (CPX 1)** — `InspectEntityPowerAsync_PrioritizesNonResourceEntities` test asserted `table.sort(entities` but the implementation was refactored to use the `sort_entities()` helper. Updated assertion to `sort_entities(entities`.
- [x] **Building Validation Deletes All Buildings on RCON Error (CPX 1)** — `ParseValidationResponse` in `BuildingMemoryService` treated any non-`"1"` CSV element as false (building missing). Lua error strings caused `Split(',')` to produce garbage, deleting all tracked buildings. Fixed: returns null on invalid format (empty, non-0/1 values); caller returns error JSON instead of deleting.
- [x] **`PlaceEntityAsync` Loses Item on `create_entity` Failure (CPX 1)** — Lua called `remove_item` then `create_entity` without checking return value. If placement failed, item was permanently lost. Fixed: stores `create_entity` result, restores item via `player.insert` if nil.
- [x] **`int.Parse` Crash on Invalid RCON Port Config (CPX 1)** — `RconConnectionService` used `int.Parse` outside try/catch for `FACTORIO_RCON_PORT`. Non-numeric values threw unhandled `FormatException`. Fixed: `int.TryParse` with fallback to 27015.
- [x] **Broken XML Doc on `InsertItemsAsync` (CPX 1)** — Missing `/// <summary>` opening tag on `InsertItemsAsync` in `FactorioService.Entity.cs`. Added the missing tag.
- [x] **Verified `walking_state` Supports 16-Direction Values (CPX 1)** — Live-tested all 8 cardinal and diagonal directions using Factorio 2's 16-direction enum values (0,2,4,6,8,10,12,14). All directions arrive successfully: North(0), Northeast(2), East(4), Southeast(6), South(8), Southwest(10), West(12), Northwest(14). `walking_state = {walking=true, direction=d}` accepts the full 16-direction range without issue. One SW test hit an obstacle (pipes) and returned "stuck" — retrying in open space succeeded immediately, confirming the stuck was path-related, not direction-related.
- [x] **MCP Tools Error-on-Success Resolved (CPX 2)** — Live-tested all 4 affected tools (`place_entity`, `set_goal`, `insert_between`, `place_ghost_entity`) — all returned proper structured JSON without MCP SDK errors. The bug was not consistently reproducible and appears to have been fixed by prior refactoring (JSON parsing, coordinate standardization, Lua string escaping). Confirmed resolved through live testing.

- [x] **Direction Oscillation on Straight-Line Paths (CPX 2)** — `CalculateDirection` in `PathfindingService.cs` caused direction to flip-flop between adjacent octants when walking straight lines with small lateral drift. Added hysteresis with a ~7.5° dead zone: cardinal directions use `tan(60°)` relaxed threshold to resist switching to diagonal, diagonal directions use `tan(75°)` strict threshold to resist switching to cardinal. Extracted `ComputeOctant` helper. 7 new hysteresis tests.
- [x] **`sort_entities` Doesn't Sort by Distance (CPX 2)** — `LuaEntitySort` only deprioritized resource entities and player character but didn't sort remaining entities by distance from the query position. Fixed: `sort_entities(t, qx, qy)` now accepts query coordinates and sorts non-resource entities by Euclidean distance. Updated all 12 call sites.
- [x] **`PlaceEntitySmartAsync` Grid Alignment (CPX 2)** — Spiral search used integer offsets, so 3×3 entities (electric-mining-drill, assembling-machine) snapped to wrong positions. Fixed: queries `prototypes.entity[name].tile_width`/`tile_height`, snaps search center to entity grid (odd dimensions → half-integer, even → integer), direction-aware width/height swap for east/west placement.
- [x] **PlaceEntity/PlaceEntitySmart Underground Belt Auto-Type (CPX 2)** — `create_entity` was called without `type` parameter, so underground belt output (exit) ends always defaulted to "input". Fixed: Lua now checks `prototypes.entity[name].type == "underground-belt"`, searches for an unpaired input belt of the same name/direction within `max_underground_distance` in the upstream direction, and places as "output" if found. Reports `belt_type` in JSON response. Applied to both `PlaceEntityAsync` and `PlaceEntitySmartAsync`.
- [x] **InspectEntity Missing Underground Belt Type (CPX 1)** — `InspectEntityAsync` didn't report `belt_to_ground_type` for underground belts, making it impossible for the AI to distinguish input (going-in) from output (coming-out) ends. Added `belt_to_ground_type` field to the JSON output when `e.type == "underground-belt"`.

---

## Code Cleanup

- [x] **Removed WalkForDuration Tool (CPX 2)** — Removed `WalkForDuration` MCP tool from `MovementTools.cs`. It encouraged bad AI behavior (walking for a fixed duration instead of toward a target). AI should use `WalkToPosition` instead.
- [x] **Split Oversized Test File (CPX 4)** — `FactorioServiceTests.cs` (2356 lines) exceeded the 900-line limit. Removed duplicated test methods that were already extracted to partial files (Research, Wait, Interaction, Transfer). Created three new partial files: `FactorioServiceTests.Movement.cs` (Walk, StopWalking, GetPlayerPosition), `FactorioServiceTests.Inventory.cs` (GetInventory, Craft, GetCraftingQueue), `FactorioServiceTests.Entity.cs` (PlaceEntity, MineEntity, GetNearbyEntities, CheckDistance). Main file now contains only shared test infrastructure (CapturingRconClient, ScriptedRconClient, constructor) and cross-cutting tests. All 8 files under 500 lines. `FactorioService.cs` was already properly split into 7 partial files (131-505 lines each).
- [x] **PathfindingService Consolidation (CPX 4)** — Consolidated 3 duplicate walking implementations (MovementTools.WalkToPosition, NavigationTools.WalkToTargetAsync, TaskTools.WalkToTargetAsync) into a single `PathfindingService`. Uses Factorio's native A* pathfinder via `surface.request_path` + `on_script_path_request_finished` event for optimal route calculation with collision avoidance. Waypoint-following via `on_tick` handler with stuck detection and automatic re-pathing. Debug rendering draws path lines and waypoint circles in-game. `MovementTools`, `NavigationTools`, and `TaskTools` are now thin wrappers. Deleted `FactorioServiceTests.Movement.cs`. Updated all unit tests (MovementToolsTests, NavigationToolsTests, TaskToolsTests) and integration tests (LiveGameTests, McpToolIntegrationTests). Registered as singleton in Program.cs DI.
- [x] **Fragile JSON Parsing in Wait Methods (CPX 2)** — Replaced brittle string matching in `FactorioService.Wait.cs`: `result.Contains("\"queue\":[]")` → `TryParseJsonArray` helper, `result.Split("\"distance\":")` → `TryParseJsonDouble` helper, `result.Split("\"tick\":")` → `TryParseJsonLong` helper. New helpers follow the same pattern as existing `TryParseJsonInt`.
- [x] **TryParseJsonString Escaped Quote Handling (CPX 1)** — Rewrote `TryParseJsonString` in `FactorioService.Wait.cs` to scan for closing `"` while skipping `\"` escape sequences. Added unescape logic (`.Replace("\\\"", "\"").Replace("\\\\", "\\")`).
- [x] **Lua JSON String Escaping (CPX 2)** — Added `internal const string LuaJsonEscape` to `FactorioService.cs` defining a Lua `esc()` function (`s:gsub('\\', '\\\\'):gsub('"', '\\"')`). Applied `esc()` wrapper to all entity name/type/recipe string concatenations in JSON-building Lua scripts across 8 files: `FactorioService.Entity.cs`, `FactorioService.World.cs`, `FactorioService.Wait.cs`, `FactorioService.Research.cs`, `FactorioService.Inventory.cs`, `VisionService.cs`, `EnergyService.cs`, `MiningService.cs`.
- [x] **Coordinate Access Inconsistency & VisionService Center Fallback (CPX 1)** — Standardized all `posExpr` from array-style `{val,val}` to named-field `{x=val,y=val}` across 6 files: `FactorioService.World.cs`, `VisionService.cs`, `BlueprintService.cs`. Removed fragile `center[1] or center.x` fallback patterns and unnecessary `if type(center) ~= "table"` guard in VisionService.
- [x] **Duplicated Entity Selection Pattern (CPX 2)** — Extracted `sort_entities(t)` Lua helper function as `internal const string LuaEntitySort` in `FactorioService.cs`. Replaced all 10 inline sort implementations across `Entity.cs`, `Wait.cs`, and `EnergyService.cs` with `sort_entities()` calls.
- [x] **`LookInDirectionAsync` nil direction guard (CPX 1)** — Reviewed: `dir_names[e.direction]` with nil direction returns nil in Lua (no error), and `if dn then` correctly skips the direction field. Code is already safe and idiomatic. No change needed.
- [x] **Pathfinding Segment-Vector Projection Model (CPX 3)** — Replaced the flawed waypoint-following system (`AdvanceWaypoints` + `GetLookAheadIndex` + goal-distance stuck detection) with a segment-vector projection model. Three bugs fixed: (1) Stuck detection measured goal distance instead of path progress — player could be successfully following the A* path around obstacles while stuck counter fired because final target was temporarily farther away. Now measures cumulative projected distance along path segments. (2) Waypoint skip heuristic (`perpDist < 3.0`) could cut corners through obstacles the pathfinder routed around. Now uses strict segment projection (`t >= 1.0`) — only advances when player has mathematically passed the segment endpoint. (3) Lookahead picked the furthest valid waypoint (last-match-wins) causing oscillation in tight corridors. Eliminated entirely — player steers toward the current segment endpoint. New model: `ProjectOntoSegment` (vector projection t parameter), `AdvanceSegment` (advance when t≥1), `GetPathProgress` (cumulative segment progress for stuck detection). 34 new unit tests.
- [x] **Removed 6 Deprecated MCP Tools (CPX 2)** — Removed `InitializeChatListener`, `GetBeltDirectionHelp`, `EstimateTravelTime`, `ScanTiles`, `GetGameTick`, `GetAvailableSlots`. These were too low-level, confused the AI, or overlapped with better alternatives. Service methods kept where internally used (`GetGameTickAsync` by `WaitForTicksAsync`, `InitializeChatListenerAsync` by `RconConnectionService`). Updated AllCommands test count 42→37, removed corresponding tests, cleaned TOOLS.md and PROMPT.md.
- [x] **Removed 3 More Deprecated MCP Tools (CPX 2)** — Removed `WaitForTicks` (generic tick-wait, task-oriented waits always better), `StopWalking` (dead tool — MCP calls sequential so can only run after WalkToPosition returns, which already stops), `GetReachableEntities` (overlaps with `GetNearbyEntities(radius:6)`). Service methods removed (no internal callers). `StopAsync` removed from PathfindingService; private `StopWalkingAsync` kept (used by `FollowWaypointsAsync`). AllCommands count 37→36.

---

- [x] **Test Project Setup (CPX 1)**
- [x] **RCON Packet Serialization Tests (CPX 2)** — Extracted `ToBytes()` and `FromPayload()` serialization methods onto `RconPacket`, refactored `RconClient` to use them. 20 unit tests covering wire format (byte order, sizes, null terminators), all three packet types (Auth/ExecCommand/ResponseValue), round-trip serialization, UTF-8 bodies, edge cases (empty body, negative id, large body, too-small payload).
- [x] **FactorioService Lua Command Tests (CPX 2)** — 58 unit tests in `FactorioServiceTests.cs` verifying all service methods produce correct Lua commands. Uses `CapturingRconClient` test double (overrides virtual `ExecuteAsync`) to capture commands without TCP. Tests cover: correct Lua API calls, JSON output structure, parameter interpolation with InvariantCulture, direction/recipe handling, proximity/inventory validation inclusion, argument validation (null, whitespace, zero, negative), and `/c` prefix on all commands. Made `RconClient.ExecuteAsync` virtual and unsealed the class for testability.
- [x] **MCP Tool Integration Tests (CPX 3)** — 34 tests in `Tests/Tools/McpToolIntegrationTests.cs` verifying all 9 tool classes (MovementTools, InventoryTools, EntityTools, WorldTools, InteractionTools, WaitTools, ChatTools, GoalTools, EnergyTools). DI resolution tests verify each tool class can be constructed from a ServiceProvider with the same registrations as Program.cs. Delegation tests verify each tool method passes parameters correctly to the underlying service (FactorioService, EnergyService, or GoalPlannerService) by checking RCON commands or JSON responses.
- [x] **RCON Integration Tests (CPX 2)** — `Tests/Integration/RconIntegrationTests.cs` with 12 tests covering RCON protocol against a live Factorio instance: authentication (success, wrong password, wrong host), command execution (simple, JSON output, empty output, Lua errors, sequential, large output), game state queries (version, connected players), and reconnection after disconnect. All tests marked with `[Trait("Category", "Integration")]` for CI exclusion via `--filter "Category!=Integration"`. Also added same trait to existing `LiveGameTests`.

## Low Priority Features (2026-04)

- [x] **Power Network Topology (CPX 3)** — Added `GetPowerNetworkTopologyAsync` to `EnergyService` and `GetPowerNetworkTopology` MCP tool to `EnergyTools`. Groups all electric poles by `electric_network_id`, maps pole-to-pole neighbour adjacency via `neighbours.copper`, and lists producers (`energy_generated_last_tick > 0`) and consumers per network segment. Returns structured JSON with `network_count`, per-network `poles`, `producers`, and `consumers` arrays.
- [x] **Combat Tools (CPX 3)** — New `CombatService.cs` + `CombatTools.cs` with 2 MCP tools: `ScanEnemies` (find enemy units via `find_enemy_units`, spawners and worms via `find_entities_filtered{force="enemy"}`, and nearest military target via `find_nearest_enemy`) and `GetDefenses` (find player ammo/fluid/electric turrets, read ammo inventory, kill count, and shooting target). Registered in `Program.cs`. 20 unit tests in `CombatServiceTests.cs`.
- [x] **Train Management Tools (CPX 3)** — New `TrainService.cs` + `TrainTools.cs` with 4 MCP tools: `GetTrains` (list all trains on current surface with state, position, speed, cargo), `GetTrainStops` (list all stations with docked train), `InspectTrain` (detailed per-train view including schedule records and cargo), `SetTrainMode` (toggle manual/automatic mode). Uses `game.train_manager.get_trains`, `get_train_stops`, `get_train_by_id`, `LuaSchedule.get_records`. Registered in `Program.cs`. 20 unit tests in `TrainServiceTests.cs`.
- [x] **Logistics Tools (CPX 3)** — New `LogisticsService.cs` + `LogisticsTools.cs` with 3 MCP tools: `GetLogisticNetwork` (stats for network at player's position: robot counts, provider/requester/storage counts), `GetNetworkContents` (all items in the network via `LuaLogisticNetwork.get_contents`), `GetRobotStatus` (idle vs. busy logistic and construction robot counts with sample positions). Uses `surface.find_logistic_network_by_position`. Registered in `Program.cs`. 19 unit tests in `LogisticsServiceTests.cs`.
- [x] **Logistics Flow Tracking (CPX 4)** — New `FlowService.cs` + `FlowTools.cs` with 2 MCP tools: `GetFlowGraph` (directed item-flow graph for belts/inserters/mining drills in an area — edges with `from_entity`, `to_entity`, and connection type `inserter_pickup`/`inserter_drop`/`belt`/`drill_output`) and `TraceItemFlow` (BFS from a starting entity following belt outputs and inserter drops up to configurable depth — returns node/edge lists with depth labels). Registered in `Program.cs`. 20 unit tests in `FlowServiceTests.cs`.
